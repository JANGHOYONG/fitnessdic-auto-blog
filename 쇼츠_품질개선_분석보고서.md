# 유튜브 쇼츠 자동화 품질 개선 분석 보고서

> 분석 대상: `scripts/shorts-generator.js` v11  
> 분석일: 2026-05-07  
> 목표: 조회수·구독자·좋아요 증가를 위한 쇼츠 품질 최적화

---

## 1. 현재 구조 요약

| 항목 | 현재 설정 |
|------|-----------|
| 영상 길이 | ~25~35초 (5슬라이드, 오디오 기반 가변) |
| 스크립트 생성 | GPT-4o-mini (반전형/사례형/발견형/비교형 4구조) |
| 이미지 | Pexels API ("fit woman" 계열 검색) |
| TTS | Google Wavenet-D (ko-KR), speakingRate 1.2 |
| 영상 합성 | Puppeteer(HTML→PNG) + FFmpeg |
| 자동 실행 | ❌ 수동 트리거만 (workflow_dispatch) |
| 업로드 후 처리 | 썸네일 교체 + 댓글로 블로그 링크 |

---

## 2. 발견된 버그 (즉시 수정 필요)

### 🔴 버그 1 — `supplement` 주제 완전 누락
**현상:** `TOPIC_CONFIG`와 `TOPIC_WORDS` 양쪽 모두에서 `supplement` 주제가 없음.  
→ "단백질보충제", "다이어트식품" 관련 블로그 글로 쇼츠를 만들면 **항상 `weightloss`(체중감량)로 오분류**됨.  
→ 이미지 쿼리·키워드 방향이 완전히 틀려짐.

```javascript
// shorts-generator.js 에 추가 필요
// TOPIC_CONFIG:
supplement: { queries: [
  'protein powder supplement gym woman bright',
  'healthy supplements vitamins colorful bright',
  'woman protein shake fitness bright studio'
]},

// TOPIC_WORDS:
supplement: ['단백질보충제', '다이어트식품', '영양제', '보충제', '프로틴'],
```

### 🔴 버그 2 — 자동 스케줄 없음
**현상:** `shorts-auto.yml`에 `schedule: cron` 설정이 전혀 없음.  
→ 매일 자동 발행이 **불가능**. 매번 GitHub Actions에서 수동 실행해야 함.  
→ CLAUDE.md 계획(`10:00 KST 쇼츠 1개/일`)과 완전히 불일치.

```yaml
# shorts-auto.yml에 추가 필요
on:
  schedule:
    - cron: '0 1 * * *'   # 매일 10:00 KST (UTC 01:00)
  workflow_dispatch:       # 수동 실행도 유지
```

### 🔴 버그 3 — TTS 음성이 남성 목소리 (`Wavenet-D`)
**현상:** 쇼츠는 `ko-KR-Wavenet-D`(남성)를 사용하는데, 롱폼은 `ko-KR-Standard-C`(여성)을 사용.  
→ 같은 채널에서 쇼츠와 롱폼의 목소리가 달라 **브랜드 일관성 훼손**.  
→ 30~50대 여성 타겟 채널에 남성 목소리는 반응률 저하 요인.

```javascript
// 권장 수정
voice: { languageCode: 'ko-KR', name: 'ko-KR-Wavenet-C' }, // 여성 Wavenet
// 또는 롱폼과 통일:
voice: { languageCode: 'ko-KR', name: 'ko-KR-Standard-C' },
```

---

## 3. 영상 품질 개선 포인트

### 🟠 개선 1 — TTS 속도 1.2배속 → 0.95배속
**문제:** 30~50대 타겟 시청자에게 1.2배속은 너무 빠름.  
→ 정보를 제대로 흡수하기 전에 슬라이드가 넘어가 **이탈률 상승** 원인.  
→ 특히 "구체적 수치/운동명"이 포함된 3~4번 슬라이드에서 치명적.

**권장:** `speakingRate: 0.95` (자연스럽고 신뢰감 있는 속도)  
- 영상 길이: 25~35초 → 약 30~40초로 증가 (Shorts 60초 이내 충분히 여유)

### 🟠 개선 2 — GPT가 생성하는 `hookText` 필드가 실제 영상에 전혀 표시되지 않음
**문제:** 프롬프트에서 `hookText`(썸네일 강조 문구, 8~12자)를 요청하지만,  
`makeOverlayHtml()`과 `makeThumbnailHtml()` 어디에도 `script.hookText`를 사용하는 코드가 없음.  
→ 썸네일에 `slides[0].keyword`만 표시 중 — 더 강력한 `hookText`가 낭비되고 있음.

**권장:** 썸네일 HTML의 키워드 영역을 `hookText`로 교체하고, `slides[0].keyword`는 부제로 활용.

### 🟠 개선 3 — 이미지 단조로움 (모든 슬라이드가 "fit woman" 계열)
**문제:** 5개 슬라이드 모두 유사한 인물 사진 → 시각적 피로도 증가.  
→ 특히 `nutrition` 주제에서도 식품 이미지 대신 사람 사진이 나오는 경우 발생.

**권장:**
- 슬라이드 타입별 이미지 전략 차별화
  - `hook`: 임팩트 있는 인물 클로즈업
  - `body 2~3번`: 메커니즘 설명 → 관련 식품/운동 동작 이미지
  - `body 4번` (실천): 구체적 식품명·운동명 그대로 검색어로 사용
  - `ending`: 밝고 성공적인 분위기

### 🟠 개선 4 — 아웃트로 슬라이드에 배경 이미지 없음
**문제:** `makeOutroHtml()`은 `overlay=0:0`으로 배경 이미지 위에 합성되지만,  
현재 코드는 아웃트로용 `imagePath`를 따로 준비하지 않음 → 마지막 슬라이드 이미지 재사용 또는 검정 배경.  
→ 마무리 인상이 약해짐.

**권장:** 아웃트로 전용 이미지 쿼리 추가 (`"fit woman success achievement bright smile"`)

---

## 4. 대본(스크립트) 품질 개선

### 🟡 개선 5 — 블로그 요약(`excerpt`)이 짧으면 GPT 할루시네이션 발생
**문제:** `post.excerpt`만 GPT에 전달하는데, excerpt가 짧거나 없으면  
GPT가 "블로그 내용에 없는 정보를 지어내지 말 것" 지시를 어기고 임의 내용을 생성함.

**권장:** `post.content` 앞 500자를 추가로 전달하거나, excerpt 최소 길이 검증 추가.

```javascript
const context = post.excerpt || (post.content || '').slice(0, 500);
```

### 🟡 개선 6 — `scriptType` 로그에만 출력, 실제 활용 없음
**문제:** GPT가 4가지 구조(사례형/반전형/발견형/비교형)를 결정해서 반환하지만,  
시각적 레이아웃은 항상 동일 → 구조 정보가 낭비됨.

**권장:** 구조별로 훅 배지 문구·색상을 다르게 변경:
- 반전형: 🔄 "알고 있던 게 틀렸다?"
- 사례형: 👤 "실제 사례 공개"
- 발견형: 🔬 "전문가도 놀란 사실"
- 비교형: ⚡ "어느 쪽이 더 효과적?"

### 🟡 개선 7 — 연속된 동일 주제 방지 로직 없음
**문제:** `shortsGenerated: false` → `publishedAt: desc` 순으로 선택하는데,  
블로그 글이 같은 주제로 연속 발행되면 쇼츠도 같은 주제가 연속으로 올라감.

**권장:** 최근 3개 쇼츠의 주제(`category`)를 확인하여 다른 주제 우선 선택하는 로직 추가.

---

## 5. YouTube 최적화 (구독자·좋아요 증가)

### 🟡 개선 8 — 설명문(description) SEO 최적화 미흡
**현상:** description에 블로그 링크 + 태그 나열 수준.  
→ YouTube 검색 노출에 유리한 핵심 키워드 활용 기회 낭비.

**권장 description 구조:**
```
[1줄 핵심 요약 - 주 키워드 포함]

📌 이 영상에서 알 수 있는 것:
• [핵심 포인트 1]
• [핵심 포인트 2]
• [핵심 포인트 3]

📖 전체 내용: [블로그 링크]

─────────────────────
[#태그들]
```

### 🟡 개선 9 — 카테고리 설정 재검토
**현상:** `categoryId: '26'` (How-to & Style)  
→ 피트니스·다이어트 콘텐츠는 `'17'` (Sports)이 더 적합.  
→ 관련 추천 알고리즘에 더 잘 노출될 수 있음.

### 🟡 개선 10 — 영상 마지막에 "좋아요 유도" 자막 없음
**현상:** 아웃트로에 "저장하세요", "구독+알림" 문구는 있으나 **"좋아요"** 유도 문구가 없음.  
→ YouTube 알고리즘에서 좋아요는 구독만큼 중요한 신호.

**권장:** 아웃트로 자막에 `"❤️ 좋아요 눌러주시면 더 많은 분께 도움이 돼요"` 추가.

---

## 6. 안정성 개선

### 🟢 개선 11 — Unsplash 폴백 미활용
**현상:** `UNSPLASH_ACCESS_KEY`가 GitHub Secrets에 등록되어 있으나,  
shorts-generator.js는 이를 전혀 사용하지 않음.  
→ Pexels에서 이미지를 못 찾으면 `'fit woman fitness workout bright'`로만 재시도.

**권장:** Pexels 실패 시 Unsplash API로 폴백 시도하는 로직 추가.

### 🟢 개선 12 — 영상 길이 검증 후 DB 기록
**현상:** 최종 영상의 실제 재생시간을 DB에 저장하지 않음.  
→ 어떤 영상이 얼마나 길었는지 추적 불가 → 성과 분석 어려움.

---

## 7. 우선순위 요약

| 우선순위 | 항목 | 예상 임팩트 |
|---------|------|------------|
| 🔴 즉시 수정 | 버그 1: supplement 누락 | 7개 주제 중 1개 오작동 수정 |
| 🔴 즉시 수정 | 버그 2: 자동 스케줄 추가 | 매일 자동 업로드 실현 |
| 🔴 즉시 수정 | 버그 3: TTS 여성 목소리로 통일 | 브랜드 일관성 + 타겟 적합성 |
| 🟠 이번 주 | 개선 1: TTS 속도 0.95배 | 정보전달력 + 완시청률 ↑ |
| 🟠 이번 주 | 개선 2: hookText 썸네일에 활용 | CTR(클릭률) ↑ |
| 🟠 이번 주 | 개선 3: 이미지 다양성 | 시각적 품질 ↑ |
| 🟡 다음 주 | 개선 5: content 활용 | 할루시네이션 감소 |
| 🟡 다음 주 | 개선 6: scriptType 시각 활용 | 영상 개성 + 다양성 |
| 🟡 다음 주 | 개선 8: description SEO | 검색 노출 ↑ |
| 🟡 다음 주 | 개선 9: 카테고리 17번 | 알고리즘 노출 ↑ |
| 🟡 다음 주 | 개선 10: 좋아요 유도 | 좋아요 수 ↑ |
| 🟢 여유 시 | 개선 7: 주제 로테이션 | 주제 다양성 |
| 🟢 여유 시 | 개선 11: Unsplash 폴백 | 안정성 ↑ |

---

## 8. 다음 단계 제안

위 개선사항들을 실제 코드로 반영할 준비가 되면 아래 순서로 진행하면 됩니다:

1. **Phase 1 (버그 수정)**: supplement 추가 + 스케줄 cron 추가 + TTS 음성 수정
2. **Phase 2 (품질 향상)**: TTS 속도 + hookText 활용 + 이미지 쿼리 개선 + description 최적화
3. **Phase 3 (성과 추적)**: YouTube Analytics API 연동 → 어떤 주제/구조가 잘 되는지 데이터 기반 최적화

---
*작성: 자동화 코드 분석 | 2026-05-07*
